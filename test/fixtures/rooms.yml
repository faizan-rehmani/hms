# == Schema Information
#
# Table name: rooms
#
#  id                :bigint           not null, primary key
#  floor             :integer
#  has_multiple_beds :boolean
#  ward              :string
#  created_at        :datetime         not null
#  updated_at        :datetime         not null
#

one:
  floor: 1
  ward: MyString
  has_multiple_beds: false

two:
  floor: 1
  ward: MyString
  has_multiple_beds: false
